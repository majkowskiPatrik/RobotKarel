<%= form_for(@actor) do |f| %>
  <% if @actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% @actor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :source_code %><br />
    <%= f.text_area :source_code, :size => "80x30" %>
  </div>
  <div class="field">
    <%= f.label :static_code %><br />
    <%= f.text_area :static_code, :size => "80x30"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
